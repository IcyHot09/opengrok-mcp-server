name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          registry-url: 'https://registry.npmjs.org'

      - run: npm config delete always-auth --location=user 2>/dev/null || true
      - run: npm ci
      # grammars/ is gitignored: populate it so tree-sitter tests resolve grammars
      # (out/grammars/ from compile is a fallback, also probed by getGrammarDir).
      - run: npm run copy-grammars
      - run: npm run lint
      - run: npm run compile
      - run: npm run test:coverage
      - run: npm run package

      - name: Build VSIX
        run: npm run vsix

      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build release body
        run: node scripts/generate-release-notes.js "${{ steps.version.outputs.version }}" > release-body.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          body_path: release-body.md
          fail_on_unmatched_files: true
          files: |
            opengrok-mcp-server-${{ steps.version.outputs.version }}.vsix

      - name: Check if npm version already published
        id: npm-check
        run: |
          if npm view "opengrok-mcp-server@${{ steps.version.outputs.version }}" version >/dev/null 2>&1; then
            echo "already=true" >> "$GITHUB_OUTPUT"
          else
            echo "already=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        if: steps.npm-check.outputs.already != 'true'
        run: |
          chmod +x out/server/main.js
          npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
